import React, { Component } from 'react';
import App from '../component/AppHeader';

class AppContainer extends Component {
  render() {
    return (
      <App />
    );
  }
}

export default AppContainer;